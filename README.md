# mlops-pucminas


## Create venvs
python -m venv ./venv
venv\Scripts\activate

python.exe -m pip install --upgrade pip
pip install numpy
pip install scipy

pip install pytest


pip freeze > requirements.txt